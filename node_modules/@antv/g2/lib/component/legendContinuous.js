"use strict";
var __rest = (this && this.__rest) || function (s, e) {
    var t = {};
    for (var p in s) if (Object.prototype.hasOwnProperty.call(s, p) && e.indexOf(p) < 0)
        t[p] = s[p];
    if (s != null && typeof Object.getOwnPropertySymbols === "function")
        for (var i = 0, p = Object.getOwnPropertySymbols(s); i < p.length; i++) {
            if (e.indexOf(p[i]) < 0 && Object.prototype.propertyIsEnumerable.call(s, p[i]))
                t[p[i]] = s[p[i]];
        }
    return t;
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.LegendContinuous = void 0;
const gui_1 = require("@antv/gui");
const scale_1 = require("@antv/scale");
const d3_format_1 = require("d3-format");
const utils_1 = require("./utils");
function inferContinuousConfig(scales, options) {
    const colorScale = (0, utils_1.scaleOf)(scales, 'color');
    const { domain, range } = colorScale.getOptions();
    const { length = exports.LegendContinuous.props.defaultLength } = options;
    const [min, max] = [domain[0], domain.slice(-1)[0]];
    if (colorScale instanceof scale_1.Threshold) {
        const thresholds = colorScale.thresholds;
        // for quantize, quantile scale
        if (colorScale instanceof scale_1.Quantize || colorScale instanceof scale_1.Quantile) {
            return {
                data: [min, ...thresholds, max].map((value, index) => ({
                    value: value / max,
                    label: value,
                })),
                color: range,
            };
        }
        // for threshhold
        const data = [-Infinity, ...thresholds, Infinity].map((value, index) => ({
            value: index,
            label: value,
        }));
        return {
            data,
            color: range,
            labelFilter: (datum, index) => {
                return index > 0 && index < data.length - 1;
            },
        };
    }
    // for linear, pow, sqrt, log, time, utc scale
    const opacityScale = (0, utils_1.scaleOf)(scales, 'opacity');
    return {
        data: colorScale.getTicks().map((value) => ({ value })),
        color: new Array(length).fill(0).map((d, i) => {
            const value = ((max - min) / (length - 1)) * i + min;
            const color = colorScale.map(value);
            const opacity = opacityScale ? opacityScale.map(value) : 1;
            return color.replace(/rgb[a]*\(([\d]{1,3}), ([\d]{1,3}), ([\d]{1,3})[\S\s]*\)/, (match, p1, p2, p3) => `rgba(${p1}, ${p2}, ${p3}, ${opacity})`);
        }),
    };
}
function inferContinuousLayout(options) {
    const { position = 'top', size, length = exports.LegendContinuous.props.defaultLength, } = options;
    const layouts = {
        left: ['vertical', size, length],
        right: ['vertical', size, length],
        top: ['horizontal', length, size],
        bottom: ['horizontal', length, size],
    };
    const [orient, width, height] = layouts[position];
    return { orient, width, height };
}
/**
 * Guide Component for continuous color scale.
 * @todo Custom style.
 */
const LegendContinuous = (options) => {
    const { title, dx = 0, dy = 0, position, layout, labelFormatter } = options, rest = __rest(options, ["title", "dx", "dy", "position", "layout", "labelFormatter"]);
    return (scales, value, coordinate, theme) => {
        var _a, _b, _c;
        const { bbox } = value;
        const { x, y, width, height } = bbox;
        const finalLayout = (0, utils_1.inferComponentLayout)(position, (_c = (_b = (_a = value.scales) === null || _a === void 0 ? void 0 : _a[0]) === null || _b === void 0 ? void 0 : _b.guide) === null || _c === void 0 ? void 0 : _c.layout);
        const layoutWrapper = new utils_1.G2Layout({
            style: Object.assign({ x: x + dx, y: y + dy, width,
                height }, finalLayout),
        });
        const { continuousLegend: legendTheme = {} } = theme;
        layoutWrapper.appendChild(new gui_1.Continuous({
            style: Object.assign({}, legendTheme, Object.assign(Object.assign({ x,
                y, titleText: (0, utils_1.titleContent)(title), titleFontSize: 12, showHandle: false, showIndicator: false, labelAlign: 'value', labelFormatter: typeof labelFormatter === 'string'
                    ? (d) => (0, d3_format_1.format)(labelFormatter)(d.label)
                    : labelFormatter }, inferContinuousLayout(options)), inferContinuousConfig(scales, options)), rest),
        }));
        return layoutWrapper;
    };
};
exports.LegendContinuous = LegendContinuous;
exports.LegendContinuous.props = {
    defaultPosition: 'top',
    defaultOrientation: 'vertical',
    defaultOrder: 1,
    defaultSize: 60,
    defaultLength: 200,
};
//# sourceMappingURL=legendContinuous.js.map