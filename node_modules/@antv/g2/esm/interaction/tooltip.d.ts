import { DisplayObject } from '@antv/g';
/**
 * Show tooltip for series item.
 */
export declare function seriesTooltip(root: DisplayObject, { elements: elementsof, wait, leading, trailing, scale, coordinate, crosshairs, item, render, startX, startY, ...rest }: Record<string, any>): () => void;
/**
 * Show tooltip for non-series item.
 */
export declare function tooltip(root: DisplayObject, { elements: elementsof, scale, render, wait, leading, trailing, groupKey, }: Record<string, any>): () => void;
export declare function Tooltip(options: any): (target: any, viewInstances: any) => () => void;
