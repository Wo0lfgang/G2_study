import { Group, type DisplayObjectConfig } from '@antv/g';
import { GUI } from '../../core/gui';
import type { NavigatorStyleProps } from './types';
export type { NavigatorOptions, NavigatorStyleProps } from './types';
export declare class Navigator extends GUI<NavigatorStyleProps> {
    constructor(options: DisplayObjectConfig<NavigatorStyleProps>);
    private finishedPromise;
    private resolveFinishedPromise;
    private playState;
    private contentGroup;
    private playWindow;
    private get defaultPage();
    private innerCurrPage;
    private clipPath;
    private prevBtnGroup;
    private nextBtnGroup;
    private pageInfoGroup;
    private get pageViews();
    private get controllerShape();
    private get pageShape();
    getContainer(): Group;
    get totalPages(): number;
    get currPage(): number;
    get finished(): Promise<number>;
    getBBox(): DOMRect;
    goTo(pageNum: number): {
        finished: Promise<number>;
    };
    prev(): {
        finished: Promise<number>;
    };
    next(): {
        finished: Promise<number>;
    };
    private renderClipPath;
    private setVisiblePages;
    private adjustControllerLayout;
    private updatePageInfo;
    private getFollowingPageDiff;
    private prepareFollowingPage;
    private renderController;
    render(attributes: NavigatorStyleProps, container: Group): void;
    bindEvents(): void;
}
