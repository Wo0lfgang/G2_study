import { Group } from '@antv/g';
import { GUI } from '../../core/gui';
import type { SparklineStyleProps, SparklineOptions } from './types';
export type { SparklineStyleProps, SparklineOptions };
export declare class Sparkline extends GUI<SparklineStyleProps> {
    static tag: string;
    private static defaultOptions;
    private containerShape;
    private sparkShape;
    /**
     * 将data统一格式化为数组形式
     * 如果堆叠，则生成堆叠数据
     */
    private get rawData();
    private get data();
    private get scales();
    /**
     * 基准线，默认为 0
     */
    private get baseline();
    private get containerCfg();
    private get linesCfg();
    private get columnsCfg();
    constructor(options: SparklineOptions);
    render(attributes: SparklineStyleProps, container: Group): void;
    /**
     * 组件的更新
     */
    update(cfg: Partial<SparklineStyleProps>): void;
    /**
     * 组件的清除
     */
    clear(): void;
    /**
     * 根据数据索引获取color
     */
    private getColor;
    /**
     * 根据数据生成scale
     */
    private createScales;
}
