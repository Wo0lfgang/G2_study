import { Group } from '@antv/g';
import { GUI } from '../../core/gui';
import type { TooltipCfg, TooltipOptions } from './types';
export type { TooltipCfg, TooltipOptions };
export declare class Tooltip extends GUI<Required<TooltipCfg>> {
    static tag: string;
    private static defaultOptions;
    get HTMLTooltipElement(): HTMLElement;
    getContainer(): HTMLElement;
    set position([x, y]: [number, number]);
    private get elementSize();
    private get items();
    private get HTMLTooltipItemsElements();
    /**
     * 解析自定义内容
     */
    private get customContent();
    private element;
    private visibility;
    constructor(options: TooltipOptions);
    render(attributes: TooltipCfg, container: Group): void;
    update(cfg: Partial<TooltipCfg>): void;
    clear(): void;
    destroy(): void;
    show(): void;
    hide(): void;
    /**
     * 初始化容器
     */
    private initShape;
    /**
     * 更新 HTML 上的内容
     */
    private renderHTMLTooltipElement;
    /**
     * 根据 position 和指针位置，计算出 tooltip 相对于指针的偏移量
     * @param assignPosition {TooltipPosition} tooltip相对于指针的位置，不指定时使用默认参数
     */
    private getRelativeOffsetFromCursor;
    /**
     * 将相对于指针的偏移量生效到dom元素上
     */
    private setOffsetPosition;
    /**
     * 更新tooltip的位置
     */
    private updatePosition;
    /**
     * 计算自动调整位置后的相对位置
     * @param offsetX 根据position计算的横向偏移量
     * @param offsetY 根据position计算的纵向偏移量
     */
    private autoPosition;
}
