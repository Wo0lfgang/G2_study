import { type StandardAnimationOption } from '../../../animation';
import type { Vector2 } from '../../../types';
import { Selection } from '../../../util';
import type { AxisDatum, AxisStyleProps } from '../types';
export declare function getTickVector(value: number, cfg: AxisStyleProps): Vector2;
export declare const getTickPoints: {
    (...args: any[]): any;
    cache: Map<any, any>;
};
export declare function renderTicks(container: Selection, axisData: AxisDatum[], cfg: AxisStyleProps, callbackableStyle: any, animate: StandardAnimationOption): import("../../../animation").AnimationResult[];
