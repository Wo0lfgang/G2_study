import type { DisplayObject } from '@antv/g';
import { AxisStyleProps, RotateOverlapCfg } from '../types';
export type Utils = {
    rotate: (label: DisplayObject, rotate: number | string) => void;
};
export default function adjustAngle(labels: DisplayObject[], overlapCfg: RotateOverlapCfg, cfg: AxisStyleProps, utils: Utils): void;
