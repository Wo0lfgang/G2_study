import type { DisplayObjectConfig, Group } from '@antv/g';
import { GUI } from '../../core/gui';
import type { CategoryOptions, CategoryStyleProps } from './types';
export type { CategoryOptions, CategoryStyleProps };
export declare class Category extends GUI<CategoryStyleProps> {
    constructor(config: DisplayObjectConfig<CategoryStyleProps>);
    private titleGroup;
    private title;
    private itemsGroup;
    private items;
    private renderTitle;
    private renderItems;
    private adjustLayout;
    private get availableSpace();
    getBBox(): DOMRect;
    render(attributes: CategoryStyleProps, container: Group): void;
}
