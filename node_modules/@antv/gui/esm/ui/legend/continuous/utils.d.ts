export declare function getBlockColor(partition: number[], color: string[], orient: string): any;
export declare function getNextTickValue(ticks: number[], value: number): {
    tick: number;
    range: number[];
};
