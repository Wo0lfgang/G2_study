import { type GroupStyleProps, type PathStyleProps, type RectStyleProps } from '@antv/g';
import type { PrefixedStyle } from '../../../types';
export type Interpolate<T = string> = (val: number) => T;
export type RibbonStyle = PrefixedStyle<PathStyleProps, 'selection'> & PrefixedStyle<RectStyleProps, 'track'> & {
    size: number;
    len: number;
};
export type RibbonCfg = {
    type?: 'size' | 'color';
    orient?: 'horizontal' | 'vertical';
    color: string[] | Interpolate;
    /** select area, 0~1 */
    range?: [number, number];
    block?: boolean;
    /** partition of the block ,the length of it is the block count */
    partition?: number[];
};
export type RibbonStyleProps = GroupStyleProps & RibbonStyle & RibbonCfg;
export declare const Ribbon: new (c: import("@antv/g").DisplayObjectConfig<RibbonStyleProps>) => import("../../../util").GUI<RibbonStyleProps>;
