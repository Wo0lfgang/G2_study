import type { Group } from '@antv/g';
import { GUI } from '../../core/gui';
import type { GUIOption } from '../../types';
import type { CheckboxStyleProps, CheckboxOptions } from './types';
export type { CheckboxStyleProps, CheckboxOptions };
export declare class Checkbox extends GUI<Required<CheckboxStyleProps>> {
    /**
     * 组件 checkbox
     */
    static tag: string;
    /**  checkbox 的背景方框组件 */
    private checkboxBoxShape;
    /** 值 */
    private checked;
    /**
     * 默认配置项
     */
    static defaultOptions: GUIOption<CheckboxStyleProps>;
    constructor(options: CheckboxOptions);
    render(attributes: CheckboxStyleProps, container: Group): void;
}
