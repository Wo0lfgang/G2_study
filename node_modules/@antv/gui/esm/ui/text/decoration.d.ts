import { DisplayObject } from '@antv/g';
import type { DecorationCfg, DecorationOptions } from './types';
export declare class Decoration extends DisplayObject<Required<DecorationCfg>> {
    static tag: string;
    private static defaultOptions;
    /**
     * 得到规范的修饰线类型
     */
    private get decorationType();
    /**
     * 双线间隔
     */
    private get doubleSpacing();
    /** 上中下悬挂比例  */
    private get hangingRate();
    private get dashedCfg();
    private get dottedCfg();
    private get solidCfg();
    private get decorationCfg();
    constructor(options: DecorationOptions);
    update(cfg: Partial<DecorationCfg>): void;
    clear(): void;
    /**
     * 根据位置计算线条起始坐标
     */
    private getLinePos;
    /**
     * 根据位置创建直线路径
     */
    private getLinePath;
    /**
     * 根据位置创建双直线路径
     */
    private getDoubleLinePath;
    /**
     * 根据位置创建波浪线路径
     */
    private getWavyPath;
}
