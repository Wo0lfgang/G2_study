import { Group } from '@antv/g';
import { GUI } from '../../core/gui';
import type { TagStyleProps, TagOptions } from './types';
export type { TagStyleProps, TagOptions };
/**
 * 带文本、图标的 Tag 组件，支持 iconfont 组件
 *
 * 组成元素：Marker + Text + BackgroundRect
 */
export declare class Tag extends GUI<Required<TagStyleProps>> {
    /**
     * 标签类型
     */
    static tag: string;
    static defaultOptions: {
        style: {
            padding: number;
            spacing: number;
        };
    };
    constructor(options: TagOptions);
    render(attributes: TagStyleProps, container: Group): void;
}
