import { Group } from '@antv/g';
import { GUI } from '../../core/gui';
import type { ButtonCfg, ButtonOptions } from './types';
export type { ButtonCfg, ButtonOptions };
export declare class Button extends GUI<ButtonCfg> {
    /**
     * 组件类型
     */
    static tag: string;
    /**
     * 默认参数
     */
    private static defaultOptions;
    /**
     * 文本
     */
    private textShape;
    private state;
    private get markerSize();
    private get textAvailableWidth();
    private get buttonHeight();
    constructor(options: ButtonOptions);
    /**
     * 根据size、type属性生成实际渲染的属性
     */
    private getStyle;
    render(attributes: ButtonCfg, container: Group): void;
    /**
     * 组件的更新
     */
    update(cfg?: Partial<ButtonCfg>): void;
    /** 更新状态 (不需要走 update) */
    setState(state: 'disabled' | 'active' | 'default'): void;
    hide(): void;
    show(): void;
    private clickEvents;
    private mouseenterEvent;
    private mouseleaveEvent;
    bindEvents(): void;
}
