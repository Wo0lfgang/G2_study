import { traverse } from './traverse';
export function visibility(element, visiable) {
    var value = visiable ? 'visibility' : 'hidden';
    traverse(element, function (node) {
        node.attr('visible', value);
    });
}
//# sourceMappingURL=visibility.js.map