import { DisplayObject } from '@antv/g';
import { Selection } from './selection';
export declare function ifShow<T extends Selection | HTMLElement | DisplayObject = Selection, R = any>(show: boolean, container: T, creator: (group: T) => R, removeChildren?: boolean, removeHandler?: (group: T) => any): null | R;
