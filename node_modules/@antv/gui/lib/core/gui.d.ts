import { CustomElement, DisplayObjectConfig, Group } from '@antv/g';
import type { GenericAnimation } from '../animation';
export declare abstract class GUI<T> extends CustomElement<T> {
    constructor(config: DisplayObjectConfig<T>);
    connectedCallback(): void;
    update(cfg?: Partial<T>, animate?: GenericAnimation): any;
    clear(): void;
    destroy(): void;
    attributeChangedCallback(): void;
    abstract render(attributes: T, container: Group, animate?: GenericAnimation): any;
    bindEvents(attributes: T, container: Group): void;
}
