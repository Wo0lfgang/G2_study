import { Group } from '@antv/g';
import { GUI } from '../../core/gui';
import type { BreadcrumbCfg, BreadcrumbOptions } from './type';
export type { BreadcrumbCfg, BreadcrumbOptions };
export declare class Breadcrumb extends GUI<Required<BreadcrumbCfg>> {
    /**
     * 标签类型
     */
    static tag: string;
    /**
     * 默认参数
     */
    private static defaultOptions;
    /**
     *
     * @param options
     */
    constructor(options: BreadcrumbOptions);
    render(attributes: BreadcrumbCfg, container: Group): void;
    /**
     * 组件更新
     * @param cfg
     */
    update(cfg: Partial<BreadcrumbCfg>): void;
    /**
     * 面包屑绑定事件
     * @param shape
     * @param item
     */
    private bindInnerEvents;
}
