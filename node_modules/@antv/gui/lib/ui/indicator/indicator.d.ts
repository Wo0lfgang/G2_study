import { GUI } from '../../core/gui';
import type { IndicatorOptions, IndicatorStyleProps } from './types';
export { IndicatorOptions, IndicatorStyleProps };
export declare class Indicator<T = any> extends GUI<IndicatorStyleProps<T>> {
    constructor(options: IndicatorOptions);
    private group;
    private background;
    private label;
    private point;
    private renderBackground;
    private renderLabel;
    private adjustLayout;
    private getPath;
    private createCorner;
    bindEvents(): void;
    render(): void;
}
