import { StandardAnimationOption } from '../../../animation';
import { Selection } from '../../../util';
import type { AxisDatum, AxisStyleProps } from '../types';
export declare function renderGrid(container: Selection, data: AxisDatum[], cfg: AxisStyleProps, style: any, animate: StandardAnimationOption): void;
