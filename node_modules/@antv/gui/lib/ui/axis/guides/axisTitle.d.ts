import { type StandardAnimationOption } from '../../../animation';
import { type Selection } from '../../../util';
import type { AxisStyleProps } from '../types';
export declare function renderTitle(container: Selection, axis: Selection, cfg: AxisStyleProps, style: any, animate: StandardAnimationOption): import("@antv/g-lite").IAnimation | null;
