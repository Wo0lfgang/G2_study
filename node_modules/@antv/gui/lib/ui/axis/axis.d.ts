import type { Group } from '@antv/g';
import { type GenericAnimation } from '../../animation';
import { GUI } from '../../core/gui';
import type { AxisOptions, AxisStyleProps } from './types';
export type { ArcAxisOptions, ArcAxisStyleProps, AxisOptions, AxisStyleProps, LinearAxisOptions, LinearAxisStyleProps, } from './types';
export declare class Axis extends GUI<AxisStyleProps> {
    constructor(options: AxisOptions);
    render(attributes: AxisStyleProps, container: Group, specificAnimation?: GenericAnimation): import("../../animation").AnimationResult[];
}
