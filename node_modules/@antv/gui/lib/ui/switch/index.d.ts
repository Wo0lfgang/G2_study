import type { Group } from '@antv/g';
import { GUI } from '../../core/gui';
import type { GUIOption } from '../../types';
import type { SwitchStyleProps, SwitchOptions } from './types';
export type { SwitchStyleProps, SwitchOptions };
export declare class Switch extends GUI<Required<SwitchStyleProps>> {
    /**
     * 组件 switch
     */
    static tag: string;
    /**
     *  开关
     */
    private checked;
    /**
     * 默认配置项
     */
    static defaultOptions: GUIOption<SwitchStyleProps>;
    constructor(options: SwitchOptions);
    render(attributes: SwitchStyleProps, container: Group): void;
}
