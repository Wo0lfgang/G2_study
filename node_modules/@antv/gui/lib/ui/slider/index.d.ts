import { Group } from '@antv/g';
import { GUI } from '../../core/gui';
import type { SliderOptions, SliderStyleProps } from './types';
export type { SliderStyleProps, SliderOptions };
export declare class Slider extends GUI<SliderStyleProps> {
    static tag: string;
    private static defaultOptions;
    private range;
    get values(): [number, number];
    set values(values: SliderStyleProps['values']);
    private trackShape;
    private sparklineShape;
    private foregroundGroup;
    private selectionShape;
    private startHandle;
    private endHandle;
    /**
     * 选区开始的位置
     */
    private selectionStartPos;
    /**
     * 选区宽度
     */
    private selectionWidth;
    /**
     * 记录上一次鼠标事件所在坐标
     */
    private prevPos;
    /**
     * drag事件当前选中的对象
     */
    private target;
    private animation;
    private get sparklineShapeCfg();
    private get shape();
    private get availableSpace();
    constructor(options: SliderOptions);
    getValues(): [number, number];
    /** 不触发重绘 */
    setValues(values?: SliderStyleProps['values'], animate?: boolean): void;
    private innerSetValues;
    private renderTrack;
    private renderSparkline;
    private get selectionCfg();
    private renderSelection;
    render(attributes: SliderStyleProps, container: Group): void;
    private clampValues;
    /**
     * 计算蒙板坐标和宽高
     * 默认用来计算前景位置大小
     */
    private calcMask;
    /**
     * 计算手柄的x y
     */
    private calcHandlePosition;
    /**
     * 计算手柄应当处于的位置
     * @param handleType start手柄还是end手柄
     * @returns
     */
    private calcHandleText;
    private getHandleLabelShapeCfg;
    private getHandleIconShapeCfg;
    private getHandleShapeCfg;
    private getHandleSize;
    private getOrientVal;
    private setValuesOffset;
    private getRatio;
    private dispatchCustomEvent;
    bindEvents(): void;
    private onScroll;
    private onDragStart;
    private onDragging;
    private onDragEnd;
    private onValueChange;
}
