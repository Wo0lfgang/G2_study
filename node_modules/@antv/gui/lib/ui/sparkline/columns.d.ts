import { DisplayObject, BaseStyleProps, Group } from '@antv/g';
import type { ShapeAttrs } from '../../types';
export interface IColumnCfg extends ShapeAttrs {
    width: number;
    height: number;
}
export interface IColumnsCfg extends BaseStyleProps {
    columns: IColumnCfg[][];
}
export declare class Columns extends DisplayObject<IColumnsCfg> {
    columnsGroup: Group;
    constructor({ style, ...rest }: Partial<DisplayObject<IColumnsCfg>>);
    render(): void;
    update(cfg: Partial<IColumnsCfg>): void;
    clear(): void;
}
